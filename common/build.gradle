dependencies {
    compileOnly("gg.essential:elementa-1.18.1-fabric:${project.elementa_version}") {
        exclude module: "fabric-loader"
        exclude module: "kotlin-stdlib"
        exclude module: "kotlin-stdlib-jdk8"
        exclude module: "kotlin-reflect"
    }
}
